package main

/**
	Question:
	 	Arr[] = [1,2, 3, 4, 6]
		Diff = 4

	Concept
		Formula
			Sum(S1) - Sum(S2) = diff
			Sum(S1) + Sum(S2) = Sum
			Sum(S1) = (diff + sum)/2

			Call the Count of Subset Sum with value CountOfSubsetSum(arr, Sum(S1))

**/
